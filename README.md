# science.ORF Article Retrieval System

To launch the retrieval system, run the startup script from the project root:

```bash
./run_bot.sh
```

This will start the bot using the **BM25** retrieval algorithm.


**Note:** Signal-Bot configuration is currently unavailable.